# Mapbox secret — use only on the proxy server, never in the Vite bundle
MAPBOX_TOKEN=

# Optional: proxy port (default 5000)
PORT=5000

# Frontend: origin of the Express proxy (see README)
# VITE_MAP_PROXY_URL=http://localhost:5000
